# Core
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Utilities
tqdm>=4.65.0        # barra de progreso
numpy>=1.25.0
pillow>=10.0.0      # lectura y procesado de imágenes
matplotlib>=3.8.0   # para visualizar reconstrucciones
scikit-learn>=1.3.0 # para métricas o embeddings (opcional)

# Optional - para futuras mejoras
# albumentations>=1.4.0   # augmentaciones más avanzadas
# pytorch-msssim>=1.0.0   # SSIM loss si la usas
# faiss-cpu>=1.7.4        # para búsqueda de vectores
